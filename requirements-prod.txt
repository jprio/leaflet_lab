# GeoWander — dépendances production (Flask + PostGIS)
# Usage : pip install -r requirements-prod.txt
# ~15 paquets directs vs ~150 dans requirements.txt

# --- Web ---
Flask==3.1.3
flask-cors==6.0.2
Flask-Login==0.6.3
Flask-SQLAlchemy==3.1.1
gunicorn==25.3.0

# --- Base de données ---
SQLAlchemy==2.0.49
psycopg[binary]==3.3.4
GeoAlchemy2==0.19.0

# --- Géo / GPX ---
geopandas==1.1.3
gpxpy==1.6.2
shapely==2.1.2
pandas==3.0.2

# --- Auth / HTTP / config ---
requests==2.32.5
oauthlib==3.3.1
python-dotenv==1.2.2
geocoder==1.38.1

# --- Optionnel : migrations Alembic (décommenter si besoin en prod) ---
# alembic==1.14.0
